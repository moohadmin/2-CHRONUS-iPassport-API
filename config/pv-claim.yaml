kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ${APP_NAME}-${APP_SUBGROUP}-pvc
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi